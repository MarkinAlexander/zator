# Домены, которые ломаются самим обходом zapret2 (без него работают).
# Управляется автоматически: lua/break-detector.lua + lua/break-validator.sh
# (вердикт BROKEN добавляет домен сюда). Ручные правки допустимы — файл
# перечитывается на лету (TTL 2с), рестарт не нужен. Одна строка = домен.
